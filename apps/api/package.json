{
  "name": "@kapool/api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "export DATABASE_URL=\"${DATABASE_URL:-postgresql://kapool:kapool_local_password@localhost:55432/kapool?schema=public}\"; prisma generate --schema prisma/schema.prisma && tsx watch src/main.ts",
    "build": "export DATABASE_URL=\"${DATABASE_URL:-postgresql://kapool:kapool_local_password@localhost:55432/kapool?schema=public}\"; prisma generate --schema prisma/schema.prisma && tsc -p tsconfig.build.json",
    "start": "node dist/src/main.js",
    "lint": "eslint src test prisma --config ../../eslint.config.mjs",
    "test": "export DATABASE_URL=\"${DATABASE_URL:-postgresql://kapool:kapool_local_password@localhost:55432/kapool?schema=public}\"; prisma generate --schema prisma/schema.prisma && jest --config jest.config.cjs",
    "db:migrate": "export DATABASE_URL=\"${DATABASE_URL:-postgresql://kapool:kapool_local_password@localhost:55432/kapool?schema=public}\"; prisma migrate deploy --schema prisma/schema.prisma",
    "db:seed": "export DATABASE_URL=\"${DATABASE_URL:-postgresql://kapool:kapool_local_password@localhost:55432/kapool?schema=public}\"; prisma db seed --schema prisma/schema.prisma"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  },
  "dependencies": {
    "@kapool/shared": "0.0.0",
    "@nestjs/common": "^10.4.20",
    "@nestjs/core": "^10.4.20",
    "@nestjs/platform-express": "^10.4.20",
    "@nestjs/platform-socket.io": "^10.4.22",
    "@nestjs/websockets": "^10.4.22",
    "@prisma/client": "^5.22.0",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.2",
    "socket.io": "^4.8.3",
    "socket.io-client": "^4.8.3"
  },
  "devDependencies": {
    "@nestjs/testing": "^10.4.20",
    "@types/jest": "^29.5.14",
    "@types/supertest": "^6.0.3",
    "jest": "^29.7.0",
    "prisma": "^5.22.0",
    "supertest": "^7.1.4",
    "ts-jest": "^29.4.6",
    "tsx": "^4.21.0"
  }
}
